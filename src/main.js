/*window.onload = function() {
    if (window.jQuery) {  
        // jQuery is loaded  
        $('img').css('display', none)
    } else {
        // jQuery is not loaded
        alert("Doesn't Work");
    }
}*/